<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Finance Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style type="text/tailwindcss">
        @layer utilities {
            .typewriter {
                width: 0;
                overflow: hidden;
                white-space: nowrap;
                border-right: .15em solid orange; /* Cursor */
                /* Mengubah animasi:
                   - typing sekarang punya 'forwards' untuk menahan keadaan akhir
                   - blink-caret diatur durasinya agar berhenti setelah typing selesai
                */
                animation: 
                    typing 3.5s steps(40, end) forwards,
                    blink-caret .75s step-end forwards; /* 'forwards' agar kursor menghilang */
                animation-delay: 0s, 3.5s; /* Kursor mulai berkedip setelah teks selesai diketik */
                animation-iteration-count: 1; /* Hanya satu kali animasi */
            }

            @keyframes typing {
                from { width: 0 }
                to { width: 100% }
            }

            @keyframes blink-caret {
                0%, 50% { border-color: orange; } /* Kursor terlihat */
                100% { border-color: transparent; } /* Kursor menghilang di akhir */
            }

            /* Untuk efek fade-in sederhana */
            .fade-in {
                animation: fadeIn 2s ease-in-out forwards;
            }

            @keyframes fadeIn {
                from { opacity: 0; transform: translateY(20px); }
                to { opacity: 1; transform: translateY(0); }
            }
        }
    </style>
</head>

<body class="bg-gray-50">

    <div class="flex h-screen overflow-hidden">
        <div class="w-56 bg-gradient-to-r bg-black text-white p-4 hidden md:block">
            <div class="flex items-center mb-8 space-x-2">
                <i class="fas fa-wallet text-xl"></i>
                <span class="text-lg font-semibold">Dompet Dims</span>
            </div>

            <nav class="space-y-3">
                <a href="/dashboard" class="flex items-center space-x-2 p-2 rounded hover:bg-blue-700">
                    <i class="fas fa-chart-pie w-5"></i>
                    <span class="font-bold">Dashboard</span>
                </a>
                <a href="/accounts" class="flex items-center space-x-2 p-2 rounded hover:bg-blue-700">
                    <i class="fas fa-chart-pie w-5"></i>
                    <span class="font-bold">Account</span>
                </a>
                <a href="/transactions" class="flex items-center space-x-2 p-2 rounded hover:bg-blue-700">
                    <i class="fas fa-exchange-alt w-5"></i>
                    <span class="font-bold">Transactions</span>
                </a>
                <a href="#" class="flex items-center space-x-2 p-2 rounded hover:bg-blue-700">
                    <i class="fas fa-shopping-cart"></i>
                    <span class="font-bold">Shopping</span>
                </a>
                <a href="/logout" class="block p-2 rounded hover:bg-blue-700 text-white">
                    <i class="fas fa-sign-out-alt w-5"></i>
                    <span class="font-bold">Logout</span>
                </a>
            </nav>
        </div>

        <div class="flex-1 flex flex-col overflow-hidden">
            <header class="bg-white shadow-sm p-4">
                <div class="flex items-center justify-between">
                    <div class="flex items-center">
                        <button id="mobile-menu-toggle" class="md:hidden mr-4 text-gray-500">
                            <i class="fas fa-bars text-xl"></i>
                        </button>
                        <h1 class="text-xl font-semibold">Dashboard</h1>
                    </div>
                    <div class="flex items-center space-x-3">
                        <div class="w-10 h-10 rounded-full bg-gray-200 flex items-center justify-center">
                            <a href="/userprofiles"><i class="fas fa-user text-gray-600"></i></a>
                        </div>
                    </div>
                </div>
            </header>

            <div id="mobile-menu" class="fixed inset-0 z-50 hidden bg-gray-800 bg-opacity-75">
                <div
                    class="absolute top-0 left-0 h-screen w-56 bg-blue-800 p-4 transform -translate-x-full transition-transform duration-300">
                    <div class="flex items-center justify-between mb-8">
                        <div class="flex items-center space-x-2">
                            <i class="fas fa-wallet text-xl"></i>
                            <span class="text-lg font-semibold">FinTrack</span>
                        </div>
                        <button id="mobile-menu-close" class="text-white">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                    <nav class="space-y-3">
                        <a href="#" class="block p-2 rounded bg-blue-700 text-white">
                            <i class="fas fa-chart-pie w-5"></i>
                            <span>Dashboard</span>
                        </a>
                        <a href="#" class="block p-2 rounded hover:bg-blue-700 text-white">
                            <i class="fas fa-exchange-alt w-5"></i>
                            <span>Transactions</span>
                        </a>
                        <a href="#" class="block p-2 rounded hover:bg-blue-700 text-white">
                            <i class="fas fa-piggy-bank w-5"></i>
                            <span>Budget</span>
                        </a>
                        <a href="#" class="block p-2 rounded hover:bg-blue-700 text-white">
                            <i class="fas fa-cog w-5"></i>
                            <span>Settings</span>
                        </a>
                    </nav>
                </div>
            </div>

            <main class="flex-1 p-6 overflow-y-auto flex items-center justify-center">
                <div class="bg-white p-8 rounded-lg shadow-xl text-center" style="width: fit-content; max-width: 90%;">
                    <h1 class="text-3xl md:text-4xl font-bold text-blue-600 mb-4 fade-in">Selamat Datang!</h1>
                    <div class="flex justify-center items-center">
                        <p
                            class="typewriter text-xl md:text-2xl font-semibold text-gray-800 overflow-hidden whitespace-nowrap">
                            Still in progress, be patient!
                        </p>
                    </div>
                    <p class="text-sm text-gray-500 mt-4 fade-in">Kami sedang bekerja keras untuk Anda 😒.</p>
                </div>
            </main>
        </div>
    </div>


</body>

</html>