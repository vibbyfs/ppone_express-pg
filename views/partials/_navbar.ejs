
<nav class="fixed top-0 left-0 w-full bg-white shadow-md z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
            <!-- Logo -->
            <div class="flex-shrink-0 flex items-center">
                <span class="text-2xl font-bold text-blue-700">MyLogo</span>
            </div>

            <!-- Hamburger (Mobile) -->
            <div class="flex md:hidden">
                <button id="navbar-toggle" class="text-gray-700 focus:outline-none" aria-label="Toggle menu">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M4 6h16M4 12h16M4 18h16" />
                    </svg>
                </button>
            </div>

            <!-- Menu -->
            <div id="navbar-menu"
                class="hidden md:flex md:items-center md:justify-center w-full md:w-auto transition-all duration-200">
                <ul class="flex flex-col md:flex-row md:space-x-8 md:ml-10">
                    <li><a href="/" class="block py-2 px-4 text-gray-700 hover:text-blue-600">Home</a></li>
                    <li><a href="/about" class="block py-2 px-4 text-gray-700 hover:text-blue-600">About</a></li>
                    <li><a href="/contact" class="block py-2 px-4 text-gray-700 hover:text-blue-600">Contact</a></li>
                </ul>
            </div>

            <!-- Right -->
            <div class="hidden md:flex md:items-center space-x-4">
                <a href="/login" class="py-2 px-4 rounded hover:bg-blue-100 text-blue-700 font-semibold">Login</a>
                <a href="/register"
                    class="py-2 px-4 rounded bg-blue-700 text-white font-semibold hover:bg-blue-800">Register</a>
            </div>
        </div>
        <!-- Mobile Right Menu -->
        <div id="navbar-mobile-extra" class="md:hidden hidden mt-2">
            <a href="/login" class="block py-2 px-4 rounded hover:bg-blue-100 text-blue-700 font-semibold">Login</a>
            <a href="/register"
                class="block py-2 px-4 rounded bg-blue-700 text-white font-semibold hover:bg-blue-800">Register</a>
        </div>
    </div>
</nav>

<!-- Tambahkan sedikit JS agar menu mobile bisa toggle -->
<script>
    const toggle = document.getElementById('navbar-toggle');
    const menu = document.getElementById('navbar-menu');
    const mobileExtra = document.getElementById('navbar-mobile-extra');

    toggle.addEventListener('click', () => {
        menu.classList.toggle('hidden');
        mobileExtra.classList.toggle('hidden');
    });
</script>